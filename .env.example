# Database Configuration
DB_URL=
DB_USERNAME=
DB_PASSWORD=

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=

# JWT Configuration
JWT_SECRET=

# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일 생성: cp .env.example .env
# 2. .env 파일에 실제 값 입력
# 3. .env 파일은 git에 커밋하지 말 것 (.gitignore에 등록됨)